// Added while loop demonstration in Scala.

object WhileLoop {
  def main(args: Array[String]) {
    var count = 1
    while (count <= 5) {
      println(s"While Loop Count: $count")
      count += 1
    }
  }
}